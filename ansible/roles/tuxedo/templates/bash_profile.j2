# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
        . ~/.bashrc
fi

# Source environment configuration for user logins
if [ -f {{ item.profile }} ]; then
        . {{ item.profile }}
fi
